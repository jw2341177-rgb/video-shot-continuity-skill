---
name: video-shot-continuity
description: 为 AI 广告、电商、品牌、剧情短片及多镜头视频分析并设计跨镜头连续性；适用于完整脚本、分镜表、多个镜头描述、H3 提示词或图生视频方案，重点解决人物、产品、动作、构图和镜头运动在独立生成片段之间的自然衔接，而不是单镜头美化或 H3 官方提示词格式转换。
---

# 视频镜头连续性设计

## 目标

让每个镜头既完成自己的叙事任务，也给下一镜头留下可生成、可剪辑的自然接口。故事逻辑和动作连续优先于转场技巧；能自然切就不要添加转场。

## 执行顺序

1. 先完整读取全部镜头。信息不完整时，标出未知项并采用最小必要假设；禁止先逐条孤立润色。
2. 从整条视频检查故事、人物、产品、场景、动作、运动方向、构图、景别、光线与色彩的连续性。
3. 找出最容易跳变的镜头对，区分：可以用接口改善，或必须重写故事/动作。原脚本无法自然衔接时直接指出，不用转场掩盖。
4. 将每个镜头收敛为一个主要动作；必要时拆镜，避免把转身、行走、拿取、佩戴、说话和看镜头同时塞入一镜。
5. 为每个镜头定义开始状态、核心动作、结束趋势和下一镜头起点，再选择最自然的接口。
6. 完成逐镜头设计后，做一次相邻镜头检查和整片复查。

## 连续性检查

至少核对：

- 人物：身份、外貌、服装、发型、站位、朝向、视线、手部状态和运动方向。
- 产品：外观、颜色、尺寸、开合状态、朝向、所在手、画面位置和空间关系。
- 场景：地点、时间、光线方向、色温、背景物和空间轴线。
- 摄影：主体画面位置、景别变化、机位、镜头运动方向和速度趋势。
- 动作：上一镜是否完成，下一镜从哪里继续，是否出现无理由的状态跳变。

出现方向反转、手势或产品瞬移、人物/服装突变、同一空间光线突变、无动机的大幅景别跳变时，必须显式报告。

## 接口选择

自然优先，按上下文使用一种主接口，必要时配一个辅助接口。不要每两镜都设计复杂转场，也不要全片重复同一种接口。

可选接口包括动作、视线、物体、构图、镜头运动、遮挡、景别、色彩/光线。优先用动作和物体逻辑；色彩/光线通常只作辅助。需要选择细节或扩展模式库时，读取 [references/continuity-patterns.md](references/continuity-patterns.md)。

## AI 视频写法

把结束状态写成明确的动作发展方向和画面趋势，不要求模型精确停在某一帧。例如写“后半段礼盒缓慢靠近镜头并占据更大画面”，不要只写“展示礼盒”，也不要规定不可稳定复现的像素级落点。

默认推荐 Image to Video（图生视频）。只有状态 A 到 B 必须精确、产品位置变化需要强控制、关键转场必须落点，或普通图生视频多次仍接不上时，才推荐首尾帧视频，并说明原因。涉及生成方式、连续性锁定或提示语改写时，读取 [references/ai-video-rules.md](references/ai-video-rules.md)。

## H3 职责边界

本 Skill 只负责故事连续性、镜头关系、动作设计和接口设计，不伪造 MiniMax H3 官方规则。项目中存在 `h3-prompt-writing` 时，将定稿后的结构化信息交给它转换为 H3 提示词，不修改或替代该 Skill。

交接信息至少包含：镜头任务、开始状态、核心动作、结束趋势、镜头运动、连续性锁定项、与下一镜接口、建议生成方式及理由。

## 输出格式

先输出“整体判断”：整片是否连贯、最易跳变的位置、需要重写的镜头及原因。然后逐镜头输出：

```text
镜头编号：
开始状态：
核心动作：
结束状态：
与下一镜接口：
推荐接口类型：
镜头运动：
建议生成方式：图生视频 / 首尾帧视频（如为首尾帧，说明原因）
```

最后列出跨镜头连续性锁定项。涉及 H3 时，追加“交给 h3-prompt-writing 的信息”，但不自行套用 H3 官方格式。

## 质量门槛

- 每镜只有一个清晰的主要动作。
- 每个非末镜都明确给出下一镜头接口；末镜写明收束状态。
- 接口服务于叙事，不靠淡入淡出、甩镜、闪白或黑场硬救。
- 广告重点检查产品位置、方向和外观；人物片重点检查视线、双手、朝向和运动方向。
- 设计适用于不同产品、人物或剧情，不绑定某个案例。

需要演示或回归测试时，按题材读取 [examples/](examples/) 中的案例；故障案例必须能判断“不能靠转场解决，需要重新设计镜头”。
